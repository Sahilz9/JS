const func = (a, b) => {
    return a + b;
};

const pow = (a, b) => {
    return a ** b;
};

// If i've a single argument then i don't need to put inside the brackets

const cube = n => {
    return n * n * n;
};


// Arrow function (Implicit return) makint more concise 
const mul = (a, b) => (
    a * b
);


// Here i haven't put the statement terminator nor return cz am using single value

