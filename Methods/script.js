//Action that can be performed on an object
//The function which are define inside an object called a Methods

const calculator = {
    add: function (a, b) {
        return a + b;
    },

    sub: function (a, b) {
        return a - b;
    },

    mul: function (a, b) {
        return a * b;
    }
};

//add is the key & function is the value